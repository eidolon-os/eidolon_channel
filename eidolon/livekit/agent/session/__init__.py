"""Session-scoped runtime helpers.

Import concrete helpers from their modules, for example
``session.room_data`` or ``session.interruption_orchestrator``.  This package
intentionally avoids a broad re-export facade so code structure mirrors the
runtime architecture.
"""
